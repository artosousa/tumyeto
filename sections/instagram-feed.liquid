
{% javascript %}
    // Function to fetch Instagram posts for a given username
    async function fetchInstagramPosts(username, count = 12) {
        const endpoint = `https://www.instagram.com/artosousa/?__a=1`; // Liquid variable injected
        console.log(endpoint)
        
    }
    fetchInstagramPosts()
   
{% endjavascript %}
{% schema %}
{
  "name": "Instagram Feed",
  "settings": [
    {
        "type": "text",
        "id": "username",
        "label": "Instagram Username",
        "default": "artosousa" // Optional default username
    }
  ],
  "presets": [
    {
      "name": "Instagram Feed"
    }
  ]
}
{% endschema %}
